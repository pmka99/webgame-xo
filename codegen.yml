overwrite: true
schema: './server/ApolloServer/schema.graphql'
      

generates:
  server/ApolloServer/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
  
    